import React from 'react';
import { Link } from 'react-router-dom';
import { FiSearch, FiBell } from 'react-icons/fi';

export default function Navbar() {
  return (
    <nav className='h-[80px] bg-white border-b flex items-center justify-between px-10 sticky top-0 z-50'>
      {/* Logo EMOMAP - Phóng to cỡ chữ và làm đậm hơn */}
      <Link to='/' className='font-black text-4xl text-blue-600 tracking-tighter'>
        EMOMAP
      </Link>

      {/* Menu chính ở giữa */}
      <div className='flex gap-10 font-bold text-lg text-gray-700'>
        <Link to='/home' className='hover:text-blue-500 transition-colors'>Home</Link>
        <Link to='/' className='hover:text-blue-500 transition-colors'>Feed</Link>
        <Link to='/map' className='hover:text-blue-500 transition-colors'>Map</Link>
      </div>

      {/* Nhóm Icons bên phải */}
      <div className='flex items-center gap-6'>
        <FiSearch size={26} className='text-gray-600 cursor-pointer hover:text-black' />
        <FiBell size={26} className='text-gray-600 cursor-pointer hover:text-black' />
        
        {/* Avatar - Nơi mở Auth Modal (Yêu cầu Blur nền) */}
        <Link to='/auth'>
          <div className='w-11 h-11 rounded-full bg-gray-300 border-2 border-blue-50 shadow-sm hover:scale-105 transition-transform cursor-pointer'>
          </div>
        </Link>
      </div>
    </nav>
  );
}
